// Общие переменные для всех модулей Android проекта
ext {
    // Установка версии Gradle в соответствии с build.gradle
    gradlePluginVersion = "8.1.2"
    
    // Версии Android SDK
    compileSdkVersion = 33
    targetSdkVersion = 33
    minSdkVersion = 23
    
    // Версии библиотек
    androidxAppCompatVersion = "1.6.1"
    androidxCoreVersion = "1.10.1"
    androidxMaterialVersion = "1.9.0"
    androidxBrowserVersion = "1.5.0"
    junitVersion = "4.13.2"
    androidxJunitVersion = "1.1.5"
    androidxEspressoCoreVersion = "3.5.1"
    cordovaAndroidVersion = "10.1.1"
    
    // Версии Kotlin
    kotlinVersion = "1.8.10"
    
    // Версии Firebase
    firebaseMessagingVersion = "23.3.1"
    firebaseCoreVersion = "21.1.1"
    
    // Версии OneSignal
    oneSignalVersion = "5.1.33"
}