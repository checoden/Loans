        name: Build APK

        on:
          push:
            tags: ['v*']

        jobs:
          build:
            runs-on: ubuntu-latest

            steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                node-version: '20'

            - name: Set up JDK 21
              uses: actions/setup-java@v4
              with:
                distribution: 'temurin'
                java-version: '21'

            - name: Setup Gradle optimization
              run: |
                mkdir -p ~/.gradle
                echo "org.gradle.daemon=true" >> ~/.gradle/gradle.properties
                echo "org.gradle.parallel=true" >> ~/.gradle/gradle.properties
                echo "org.gradle.caching=true" >> ~/.gradle/gradle.properties
                echo "org.gradle.configureondemand=true" >> ~/.gradle/gradle.properties
                echo "org.gradle.configuration-cache=true" >> ~/.gradle/gradle.properties
                echo "org.gradle.configuration-cache.problems=warn" >> ~/.gradle/gradle.properties
                echo "org.gradle.vfs.watch=false" >> ~/.gradle/gradle.properties
                echo "org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8" >> ~/.gradle/gradle.properties
                # Убираем ограничение workers - пусть Gradle сам определяет оптимальное количество
                # echo "org.gradle.workers.max=2" >> ~/.gradle/gradle.properties

            - name: Cache dependencies
              uses: actions/cache@v3
              with:
                path: |
                  ~/.npm
                  ~/.gradle/caches
                  ~/.gradle/wrapper
                  node_modules
                  capacitor-app/node_modules
                key: ${{ runner.os }}-build-${{ hashFiles('**/package-lock.json', '**/*.gradle*', '**/gradle-wrapper.properties') }}
                restore-keys: |
                  ${{ runner.os }}-build-

            - run: npm ci

            - name: Setup OneSignal Environment
              run: |
                echo "VITE_ONESIGNAL_APP_ID=${{ secrets.ONESIGNAL_APP_ID }}" >> $GITHUB_ENV
                echo "VITE_ONESIGNAL_REST_API_KEY=${{ secrets.ONESIGNAL_REST_API_KEY }}" >> $GITHUB_ENV

            - name: Prepare Capacitor project
              run: |
                node prepare-for-apk.js
                cd capacitor-app
                npm install
                rm -rf android
                jq 'del(.dependencies."@capacitor/android") | del(.dependencies."@capacitor/core")' package.json > package.json.tmp
                mv package.json.tmp package.json
                npm install
                mkdir -p android/gradle/wrapper
                npm install @capacitor/core@7.2.0 @capacitor/android@7.2.0 --legacy-peer-deps

            - name: Add Android platform
              run: |
                BUILD_START_TIME=$(date +%s)
                echo "🎯 ============ СОЗДАНИЕ ANDROID ПЛАТФОРМЫ ============"
                echo "📅 Время начала: $(date)"
                
                cd capacitor-app
                echo "📂 Рабочая директория: $(pwd)"
                echo "📋 Содержимое capacitor-app:"
                ls -la
                
                echo ""
                echo "🔧 СОЗДАНИЕ/СИНХРОНИЗАЦИЯ ANDROID ПЛАТФОРМЫ:"
                echo "Проверяем существующую Android структуру..."
                
                if [ -f "android/app/build.gradle" ]; then
                  echo "✅ Android платформа уже существует, выполняем синхронизацию..."
                  npx cap sync android
                else
                  echo "❌ Android платформа неполная или отсутствует"
                  echo "🗂️ Удаляем неполную структуру:"
                  rm -rf android
                  echo "🔧 Создаем полную Android платформу:"
                  npx cap add android
                  echo "🔄 Синхронизируем новую платформу:"
                  npx cap sync android
                  
                  echo "✅ Проверяем создание критически важных файлов:"
                  if [ -f "android/app/build.gradle" ]; then
                    echo "✅ app/build.gradle создан"
                  else
                    echo "❌ app/build.gradle НЕ СОЗДАН - критическая ошибка!"
                    echo "📂 Структура android/:"
                    find android -type f -name "*.gradle" | head -10
                    exit 1
                  fi
                fi
                
                echo ""
                echo "✅ ПРОВЕРКА СОЗДАННОЙ СТРУКТУРЫ:"
                echo "📂 Структура android/:"
                find android -type f -name "*.xml" -o -name "*.gradle" | head -20
                
                echo ""
                echo "📄 БАЗОВЫЙ МАНИФЕСТ ПОСЛЕ СОЗДАНИЯ:"
                if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
                  echo "✅ Манифест создан успешно"
                  echo "--- СОДЕРЖИМОЕ МАНИФЕСТА ---"
                  cat android/app/src/main/AndroidManifest.xml
                  echo "--- КОНЕЦ МАНИФЕСТА ---"
                  echo ""
                  echo "📋 Разрешения в базовом манифесте:"
                  grep "uses-permission" android/app/src/main/AndroidManifest.xml || echo "❌ Нет разрешений в базовом манифесте"
                else
                  echo "❌ Манифест не создан! СОЗДАЕМ ВРУЧНУЮ"
                  echo "📂 Содержимое android/app/src/main/:"
                  ls -la android/app/src/main/ || echo "Директория не существует"
                  
                  echo "🔧 Создаем AndroidManifest.xml с нуля..."
                  mkdir -p android/app/src/main
                  echo '<?xml version="1.0" encoding="utf-8"?>' > android/app/src/main/AndroidManifest.xml
                  echo '<manifest xmlns:android="http://schemas.android.com/apk/res/android"' >> android/app/src/main/AndroidManifest.xml
                  echo '    xmlns:tools="http://schemas.android.com/tools"' >> android/app/src/main/AndroidManifest.xml
                  echo '    package="ru.checoden.onlineloans">' >> android/app/src/main/AndroidManifest.xml
                  echo '' >> android/app/src/main/AndroidManifest.xml
                  echo '    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>' >> android/app/src/main/AndroidManifest.xml
                  echo '    <uses-permission android:name="android.permission.WAKE_LOCK" />' >> android/app/src/main/AndroidManifest.xml
                  echo '    <uses-permission android:name="android.permission.VIBRATE" />' >> android/app/src/main/AndroidManifest.xml
                  echo '    <uses-permission android:name="android.permission.INTERNET" />' >> android/app/src/main/AndroidManifest.xml
                  echo '    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />' >> android/app/src/main/AndroidManifest.xml
                  echo '' >> android/app/src/main/AndroidManifest.xml
                  echo '    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"' >> android/app/src/main/AndroidManifest.xml
                  echo '        android:label="@string/app_name" android:theme="@style/AppTheme">' >> android/app/src/main/AndroidManifest.xml
                  echo '        <activity android:exported="true" android:name="ru.checoden.onlineloans.MainActivity">' >> android/app/src/main/AndroidManifest.xml
                  echo '            <intent-filter>' >> android/app/src/main/AndroidManifest.xml
                  echo '                <action android:name="android.intent.action.MAIN" />' >> android/app/src/main/AndroidManifest.xml
                  echo '                <category android:name="android.intent.category.LAUNCHER" />' >> android/app/src/main/AndroidManifest.xml
                  echo '            </intent-filter>' >> android/app/src/main/AndroidManifest.xml
                  echo '        </activity>' >> android/app/src/main/AndroidManifest.xml
                  echo '    </application>' >> android/app/src/main/AndroidManifest.xml
                  echo '</manifest>' >> android/app/src/main/AndroidManifest.xml
                  echo "✅ AndroidManifest.xml создан с POST_NOTIFICATIONS!"
                  echo "--- СОДЕРЖИМОЕ СОЗДАННОГО МАНИФЕСТА ---"
                  cat android/app/src/main/AndroidManifest.xml
                  echo "--- КОНЕЦ МАНИФЕСТА ---"
                fi
                
                echo "📦 Копируем google-services.json..."
                cp ../google-services.json android/app/google-services.json || true
                
                echo ""
                echo "🎯 ============ СИНХРОНИЗАЦИЯ CAPACITOR ============"
                echo "📅 Время: $(date)"
                echo "🔄 Выполняем cap sync android..."
                
                npx cap sync android
                echo "✅ Синхронизация завершена"
                
                echo "📱 Устанавливаем дополнительные плагины для push уведомлений:"
                npm install @capacitor/push-notifications
                npx cap sync android
                
                echo ""
                echo "📄 ПРОВЕРКА МАНИФЕСТА ПОСЛЕ СИНХРОНИЗАЦИИ:"
                MANIFEST_PATH="android/app/src/main/AndroidManifest.xml"
                
                if [ -f "$MANIFEST_PATH" ]; then
                  echo "✅ Манифест найден после sync по пути: $MANIFEST_PATH"
                  echo "--- СОДЕРЖИМОЕ ПОСЛЕ SYNC ---"
                  cat "$MANIFEST_PATH"
                  echo "--- КОНЕЦ МАНИФЕСТА ---"
                  echo ""
                  echo "📋 Разрешения после cap sync:"
                  grep "uses-permission" "$MANIFEST_PATH" || echo "❌ Нет разрешений после cap sync"
                else
                  echo "❌ Манифест не найден по пути: $MANIFEST_PATH"
                  echo "📂 Текущая директория: $(pwd)"
                  echo "📂 Структура android/:"
                  find android -name "AndroidManifest.xml" -type f 2>/dev/null || echo "Нет файлов AndroidManifest.xml в android/"
                  echo "📂 Все манифесты в проекте:"
                  find . -name "AndroidManifest.xml" -type f 2>/dev/null || echo "Нет файлов AndroidManifest.xml"
                  echo "📂 Содержимое android/app/src/main/:"
                  ls -la android/app/src/main/ 2>/dev/null || echo "Директория не существует"
                fi
                
                echo ""
                echo "🎯 ============ ДОБАВЛЕНИЕ PUSH-РАЗРЕШЕНИЙ ============"
                echo "📅 Время: $(date)"
                echo "📝 Добавляем POST_NOTIFICATIONS разрешение к существующему манифесту..."
                
                MANIFEST_PATH="android/app/src/main/AndroidManifest.xml"
                
                # Проверяем, есть ли уже POST_NOTIFICATIONS
                if ! grep -q "POST_NOTIFICATIONS" "$MANIFEST_PATH"; then
                  echo "⚡ Добавляем POST_NOTIFICATIONS разрешение..."
                  # Добавляем разрешение перед закрывающим тегом </manifest>
                  sed -i 's|</manifest>|    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>\n</manifest>|' "$MANIFEST_PATH"
                  echo "✅ POST_NOTIFICATIONS добавлено"
                else
                  echo "✅ POST_NOTIFICATIONS разрешение уже присутствует"
                fi
                
                echo "⚡ Добавляем дополнительные push-разрешения..."
                
                # Добавляем другие push-разрешения если их нет
                if ! grep -q "WAKE_LOCK" "$MANIFEST_PATH"; then
                  echo "  + Добавляем WAKE_LOCK"
                  sed -i 's|</manifest>|    <uses-permission android:name="android.permission.WAKE_LOCK" />\n</manifest>|' "$MANIFEST_PATH"
                fi
                
                if ! grep -q "VIBRATE" "$MANIFEST_PATH"; then
                  echo "  + Добавляем VIBRATE"
                  sed -i 's|</manifest>|    <uses-permission android:name="android.permission.VIBRATE" />\n</manifest>|' "$MANIFEST_PATH"
                fi
                
                if ! grep -q "RECEIVE_BOOT_COMPLETED" "$MANIFEST_PATH"; then
                  echo "  + Добавляем RECEIVE_BOOT_COMPLETED"
                  sed -i 's|</manifest>|    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />\n</manifest>|' "$MANIFEST_PATH"
                fi
                
                if ! grep -q "com.google.android.c2dm.permission.RECEIVE" "$MANIFEST_PATH"; then
                  echo "  + Добавляем C2DM_RECEIVE"
                  sed -i 's|</manifest>|    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />\n</manifest>|' "$MANIFEST_PATH"
                fi
                
                if ! grep -q "ACCESS_NETWORK_STATE" "$MANIFEST_PATH"; then
                  echo "  + Добавляем ACCESS_NETWORK_STATE"
                  sed -i 's|</manifest>|    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />\n</manifest>|' "$MANIFEST_PATH"
                fi
                
                echo ""
                echo "✅ РАЗРЕШЕНИЯ УСПЕШНО ДОБАВЛЕНЫ:"
                echo "📋 Всего разрешений: $(grep -c "uses-permission" "$MANIFEST_PATH")"
                echo "🎯 POST_NOTIFICATIONS: $(grep -q "POST_NOTIFICATIONS" "$MANIFEST_PATH" && echo "✓" || echo "✗")"
                
                chmod +x ./create-directories.sh
                ./create-directories.sh
                cd ..
                
                echo ""
                echo "🎯 ============ НАСТРОЙКА PUSH-УВЕДОМЛЕНИЙ ============"
                echo "📅 Время: $(date)"
                echo "🔧 Запускаем setup-android-notification-channel.js..."
                
                node setup-android-notification-channel.js
                echo "✅ Скрипт настройки уведомлений завершен"
                
                echo ""
                echo "📄 КРАТКАЯ ПРОВЕРКА ПОСЛЕ НАСТРОЙКИ УВЕДОМЛЕНИЙ:"
                MANIFEST_PATH="capacitor-app/android/app/src/main/AndroidManifest.xml"
                if [ -f "$MANIFEST_PATH" ]; then
                  echo "✅ Манифест найден"
                  echo "📋 Количество разрешений: $(grep -c "uses-permission" "$MANIFEST_PATH")"
                  if grep -q "POST_NOTIFICATIONS" "$MANIFEST_PATH"; then
                    echo "✅ POST_NOTIFICATIONS присутствует"
                  else
                    echo "❌ POST_NOTIFICATIONS отсутствует"
                  fi
                else
                  echo "❌ Манифест не найден: $MANIFEST_PATH"
                fi
                
                echo "📱 Проверка MainActivity.java:"
                if [ -f "capacitor-app/android/app/src/main/java/ru/checoden/onlineloans/MainActivity.java" ]; then
                  echo "✅ MainActivity.java найден"
                  if grep -q "POST_NOTIFICATIONS" capacitor-app/android/app/src/main/java/ru/checoden/onlineloans/MainActivity.java; then
                    echo "✅ Код запроса POST_NOTIFICATIONS присутствует"
                  else
                    echo "❌ Код запроса POST_NOTIFICATIONS отсутствует"
                  fi
                else
                  echo "❌ MainActivity.java не найден!"
                fi
                
                cd capacitor-app
                node update-gradle-signing.js
                node update-android-plugin.js
                cd ..
                
                echo ""
                echo "🎯 ============ ОБНОВЛЕНИЕ ANDROID SDK ============"
                echo "📅 Время: $(date)"
                echo "🔧 Обновляем Android SDK до версии 34 (ПОСЛЕ всех Capacitor настроек)..."
                
                node update-android-sdk.cjs
                echo "✅ Android SDK обновлен"
                
                echo ""
                echo "🚨 КРИТИЧЕСКИ ВАЖНО: ФИНАЛЬНОЕ ДОБАВЛЕНИЕ POST_NOTIFICATIONS"
                echo "📅 Время: $(date)"
                echo "🔧 Добавляем POST_NOTIFICATIONS в САМОМ КОНЦЕ после всех Capacitor операций..."
                
                FINAL_MANIFEST="capacitor-app/android/app/src/main/AndroidManifest.xml"
                if [ -f "$FINAL_MANIFEST" ]; then
                  echo "✅ Финальный манифест найден"
                  
                  # Добавляем POST_NOTIFICATIONS если его нет
                  if ! grep -q "POST_NOTIFICATIONS" "$FINAL_MANIFEST"; then
                    echo "🚨 POST_NOTIFICATIONS отсутствует - ДОБАВЛЯЕМ СРОЧНО"
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>\n</manifest>|' "$FINAL_MANIFEST"
                  else
                    echo "✅ POST_NOTIFICATIONS уже есть"
                  fi
                  
                  # Добавляем остальные разрешения для push-уведомлений
                  if ! grep -q "WAKE_LOCK" "$FINAL_MANIFEST"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.WAKE_LOCK" />\n</manifest>|' "$FINAL_MANIFEST"
                  fi
                  
                  if ! grep -q "VIBRATE" "$FINAL_MANIFEST"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.VIBRATE" />\n</manifest>|' "$FINAL_MANIFEST"
                  fi
                  
                  if ! grep -q "RECEIVE_BOOT_COMPLETED" "$FINAL_MANIFEST"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />\n</manifest>|' "$FINAL_MANIFEST"
                  fi
                  
                  if ! grep -q "c2dm.permission.RECEIVE" "$FINAL_MANIFEST"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />\n</manifest>|' "$FINAL_MANIFEST"
                  fi
                  
                  if ! grep -q "ACCESS_NETWORK_STATE" "$FINAL_MANIFEST"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />\n</manifest>|' "$FINAL_MANIFEST"
                  fi
                  
                  echo "✅ Все push-разрешения добавлены в финальный манифест"
                else
                  echo "❌ КРИТИЧЕСКАЯ ОШИБКА: Финальный манифест не найден!"
                  exit 1
                fi
                
                echo ""
                echo "📋 ПРОВЕРКА НАСТРОЕК GRADLE ПОСЛЕ ОБНОВЛЕНИЯ SDK:"
                if [ -f "capacitor-app/android/app/build.gradle" ]; then
                  echo "--- НАСТРОЙКИ SDK В BUILD.GRADLE ---"
                  grep -E "(compileSdk|targetSdk|minSdk)" capacitor-app/android/app/build.gradle
                  echo "--- КОНЕЦ НАСТРОЕК SDK ---"
                else
                  echo "❌ build.gradle не найден"
                fi
                
                echo ""
                echo "🔍 ФИНАЛЬНАЯ ПРОВЕРКА ПЕРЕД СБОРКОЙ:"
                FINAL_MANIFEST="capacitor-app/android/app/src/main/AndroidManifest.xml"
                if [ -f "$FINAL_MANIFEST" ]; then
                  if grep -q "POST_NOTIFICATIONS" "$FINAL_MANIFEST"; then
                    echo "✅ POST_NOTIFICATIONS готов - можно собирать APK"
                  else
                    echo "❌ POST_NOTIFICATIONS отсутствует - КРИТИЧЕСКАЯ ОШИБКА"
                    echo "🚨 ДОБАВЛЯЕМ POST_NOTIFICATIONS ЭКСТРЕННО ПЕРЕД СБОРКОЙ"
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>\n</manifest>|' "$FINAL_MANIFEST"
                    
                    # Проверяем еще раз
                    if grep -q "POST_NOTIFICATIONS" "$FINAL_MANIFEST"; then
                      echo "✅ POST_NOTIFICATIONS успешно добавлен экстренно"
                    else
                      echo "❌ КРИТИЧЕСКАЯ ОШИБКА: Не удалось добавить POST_NOTIFICATIONS"
                      exit 1
                    fi
                  fi
                else
                  echo "❌ Манифест не найден - сборка остановлена"
                  exit 1
                fi
                
                echo "📋 Проверяем финальные настройки SDK в build.gradle:"
                if [ -f "capacitor-app/android/app/build.gradle" ]; then
                  echo "🔍 Проверяем compileSdk и targetSdk:"
                  grep -n "compileSdk" capacitor-app/android/app/build.gradle || echo "compileSdk не найден"
                  grep -n "targetSdk" capacitor-app/android/app/build.gradle || echo "targetSdk не найден"
                  grep -n "minSdk" capacitor-app/android/app/build.gradle || echo "minSdk не найден"
                else
                  echo "❌ build.gradle не найден!"
                fi
                
                cd capacitor-app

            - uses: android-actions/setup-android@v3
            - uses: gradle/gradle-build-action@v3
              with:
                gradle-version: 8.5
                build-cache-enabled: true

            - name: Verify signing configuration
              run: |
                cd capacitor-app/android
                echo "=== Проверяем настройки подписи ==="
                grep -A 10 signingConfigs app/build.gradle || echo "signingConfigs не найден!"
                ls -la app/android-keystore.keystore || echo "Keystore не найден!"

            - name: Build APK
              env:
                KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
                KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
                KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
              run: |
                cd capacitor-app/android
                echo "=== Создаем keystore файл перед сборкой ==="
                mkdir -p app
                echo "${{ secrets.KEYSTORE_B64 }}" | base64 -d > app/android-keystore.keystore
                echo "=== Проверяем местонахождение keystore ==="
                ls -la app/android-keystore.keystore
                echo "=== Проверяем содержимое build.gradle ==="
                if [ -f "app/build.gradle" ]; then
                  grep -A 5 -B 5 "storeFile" app/build.gradle || echo "storeFile не найден в build.gradle"
                else
                  echo "❌ app/build.gradle не найден!"
                  echo "📂 Содержимое app/:"
                  ls -la app/ || echo "Директория app/ не существует"
                  echo "📂 Поиск всех build.gradle файлов:"
                  find . -name "build.gradle" -type f | head -10
                fi
                if [ ! -f "app/android-keystore.keystore" ]; then
                  echo "❌ Keystore не найден в app/"
                  exit 1
                fi
                echo ""
                echo "🚨 ============ ПРИНУДИТЕЛЬНОЕ ДОБАВЛЕНИЕ РАЗРЕШЕНИЙ ============"
                echo "📅 Время: $(date)"
                echo "🔧 Запускаем force-add-permissions.js прямо перед сборкой..."
                
                cd ../..
                node force-add-permissions.js
                cd capacitor-app/android
                
                echo ""
                echo "🔍 КРИТИЧЕСКАЯ ПРОВЕРКА МАНИФЕСТА ПЕРЕД GRADLE BUILD:"
                MANIFEST_PATH="app/src/main/AndroidManifest.xml"
                if [ -f "$MANIFEST_PATH" ]; then
                  echo "✅ Манифест найден: $MANIFEST_PATH"
                  
                  echo ""
                  echo "📄 СОДЕРЖИМОЕ МАНИФЕСТА ПЕРЕД ПРОВЕРКОЙ:"
                  cat "$MANIFEST_PATH"
                  echo ""
                  
                  if grep -q "POST_NOTIFICATIONS" "$MANIFEST_PATH"; then
                    echo "✅ POST_NOTIFICATIONS уже присутствует в манифесте"
                  else
                    echo "❌ POST_NOTIFICATIONS ОТСУТСТВУЕТ - ДОБАВЛЯЕМ ПРИНУДИТЕЛЬНО"
                    # Используем более надежный способ добавления
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>\n</manifest>|' "$MANIFEST_PATH"
                    echo "✅ POST_NOTIFICATIONS добавлен через sed"
                    echo "----- ПРОВЕРКА POST_NOTIFICATIONS ПОСЛЕ ДОБАВЛЕНИЯ -----"
                    grep "POST_NOTIFICATIONS" "$MANIFEST_PATH" || echo "❌ POST_NOTIFICATIONS НЕ НАЙДЕН ПОСЛЕ ДОБАВЛЕНИЯ!"
                  fi
                  
                  # Добавляем другие критические разрешения
                  if ! grep -q "WAKE_LOCK" "$MANIFEST_PATH"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.WAKE_LOCK" />\n</manifest>|' "$MANIFEST_PATH"
                    echo "✅ WAKE_LOCK добавлен"
                    echo "----- ПРОВЕРКА WAKE_LOCK ПОСЛЕ ДОБАВЛЕНИЯ -----"
                    grep "WAKE_LOCK" "$MANIFEST_PATH" || echo "❌ WAKE_LOCK НЕ НАЙДЕН!"
                  fi
                  
                  if ! grep -q "VIBRATE" "$MANIFEST_PATH"; then
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.VIBRATE" />\n</manifest>|' "$MANIFEST_PATH"
                    echo "✅ VIBRATE добавлен"
                    echo "----- ПРОВЕРКА VIBRATE ПОСЛЕ ДОБАВЛЕНИЯ -----"
                    grep "VIBRATE" "$MANIFEST_PATH" || echo "❌ VIBRATE НЕ НАЙДЕН!"
                  fi
                  
                  echo ""
                  echo "📄 ФИНАЛЬНЫЙ МАНИФЕСТ ПОСЛЕ ВСЕХ ИЗМЕНЕНИЙ:"
                  cat "$MANIFEST_PATH"
                  echo ""
                  
                else
                  echo "❌ КРИТИЧЕСКАЯ ОШИБКА: Манифест не найден перед сборкой!"
                  exit 1
                fi
                
                echo ""
                echo "🔍 ПОСЛЕДНЯЯ ПРОВЕРКА ВСЕХ МАНИФЕСТОВ ПЕРЕД GRADLE BUILD:"
                echo "📄 Ищем все AndroidManifest.xml файлы в проекте:"
                find . -name "AndroidManifest.xml" -type f | while read manifest; do
                  echo "  📄 Найден: $manifest"
                  if grep -q "POST_NOTIFICATIONS" "$manifest"; then
                    echo "    ✅ POST_NOTIFICATIONS присутствует"
                  else
                    echo "    ❌ POST_NOTIFICATIONS отсутствует"
                    echo "    🚨 ДОБАВЛЯЕМ POST_NOTIFICATIONS в $manifest"
                    sed -i 's|</manifest>|    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>\n</manifest>|' "$manifest"
                    echo "    ✅ POST_NOTIFICATIONS добавлен"
                  fi
                done
                
                echo ""
                echo "📋 ФИНАЛЬНАЯ ПРОВЕРКА ОСНОВНОГО МАНИФЕСТА:"
                MAIN_MANIFEST="app/src/main/AndroidManifest.xml"
                if [ -f "$MAIN_MANIFEST" ]; then
                  echo "--- СОДЕРЖИМОЕ $MAIN_MANIFEST ---"
                  cat "$MAIN_MANIFEST"
                  echo "--- КОНЕЦ МАНИФЕСТА ---"
                else
                  echo "❌ Основной манифест не найден!"
                fi
                
                echo ""
                echo "🎯 ============ СБОРКА APK ============"
                echo "📅 Время начала сборки: $(date)"
                echo "✅ Keystore файл готов для сборки"
                echo "🔨 Запускаем сборку APK с оптимизацией..."
                
                ./gradlew :app:assembleRelease --build-cache --parallel --daemon --stacktrace --info
                
                echo ""
                echo "✅ СБОРКА APK ЗАВЕРШЕНА"
                echo "📅 Время завершения сборки: $(date)"
                echo ""
                echo "📂 ПРОВЕРКА СОЗДАННЫХ ФАЙЛОВ:"
                find app/build/outputs/apk -name "*.apk" -type f | while read apk; do
                  echo "  📱 Найден APK: $apk"
                  echo "     Размер: $(du -h "$apk" | cut -f1)"
                done
                
                echo ""
                echo "🔍 КРИТИЧЕСКАЯ ПРОВЕРКА РАЗРЕШЕНИЙ В СОБРАННОМ APK:"
                APK_FILE=$(find app/build/outputs/apk/release -name "*.apk" | head -1)
                if [ -f "$APK_FILE" ]; then
                  echo "Найден APK для проверки: $APK_FILE"
                  
                  # Извлекаем и проверяем манифест из APK
                  echo "Извлекаем AndroidManifest.xml из APK..."
                  unzip -p "$APK_FILE" AndroidManifest.xml > extracted_manifest.xml
                  
                  echo "Размер извлеченного манифеста: $(wc -c extracted_manifest.xml)"
                  
                  # Проверяем наличие POST_NOTIFICATIONS в бинарном манифесте
                  echo "Проверяем наличие POST_NOTIFICATIONS в APK..."
                  if strings extracted_manifest.xml | grep -q "POST_NOTIFICATIONS"; then
                    echo "✅ УСПЕХ: android.permission.POST_NOTIFICATIONS найдено в APK"
                  else
                    echo "❌ КРИТИЧЕСКАЯ ОШИБКА: POST_NOTIFICATIONS отсутствует в APK"
                    echo "Это означает что push-уведомления НЕ БУДУТ работать на Android 13+"
                    echo ""
                    echo "Содержимое всех разрешений в APK:"
                    strings extracted_manifest.xml | grep -i "permission" | sort | uniq
                    echo ""
                    echo "Полный список строк в манифесте:"
                    strings extracted_manifest.xml | head -50
                    echo "СБОРКА ОСТАНОВЛЕНА - требуется исправление"
                    exit 1
                  fi
                  
                  # Проверяем другие важные разрешения для push
                  echo ""
                  echo "Проверяем дополнительные push-разрешения:"
                  CRITICAL_PERMISSIONS=("WAKE_LOCK" "VIBRATE" "c2dm.permission.RECEIVE" "ACCESS_NETWORK_STATE")
                  MISSING_COUNT=0
                  
                  for permission in "${CRITICAL_PERMISSIONS[@]}"; do
                    if strings extracted_manifest.xml | grep -q "$permission"; then
                      echo "  ✅ $permission присутствует"
                    else
                      echo "  ⚠️ $permission отсутствует"
                      ((MISSING_COUNT++))
                    fi
                  done
                  
                  # Проверяем MainActivity.java для программного запроса разрешений
                  echo ""
                  echo "Проверяем MainActivity.java на наличие кода запроса разрешений:"
                  MAIN_ACTIVITY_PATH=$(find . -name "MainActivity.java" -type f | head -1)
                  if [ -f "$MAIN_ACTIVITY_PATH" ]; then
                    if grep -q "POST_NOTIFICATIONS" "$MAIN_ACTIVITY_PATH"; then
                      echo "  ✅ MainActivity содержит код запроса POST_NOTIFICATIONS"
                    else
                      echo "  ⚠️ MainActivity не содержит код запроса POST_NOTIFICATIONS"
                    fi
                  else
                    echo "  ❌ MainActivity.java не найден"
                  fi
                  
                  echo ""
                  if [ $MISSING_COUNT -eq 0 ]; then
                    echo "✅ ВСЕ КРИТИЧЕСКИЕ PUSH-РАЗРЕШЕНИЯ ПРИСУТСТВУЮТ В APK"
                    echo "✅ APK ГОТОВ ДЛЯ ТЕСТИРОВАНИЯ PUSH-УВЕДОМЛЕНИЙ НА ANDROID 13+"
                  else
                    echo "⚠️ Отсутствует $MISSING_COUNT дополнительных разрешений"
                    echo "✅ Но POST_NOTIFICATIONS присутствует - основная функциональность должна работать"
                  fi
                  
                  rm -f extracted_manifest.xml
                else
                  echo "❌ APK файл не найден для проверки разрешений"
                  ls -la app/build/outputs/apk/release/ || echo "Директория с APK не найдена"
                  exit 1
                fi
                
                echo ""
                echo "🎯 ============ ИТОГОВЫЙ ОТЧЕТ СБОРКИ ============"
                echo "📅 Время завершения всех этапов: $(date)"
                echo "⏱️ Продолжительность сборки: $(($(date +%s) - BUILD_START_TIME)) секунд" 
                echo ""
                echo "📊 СТАТИСТИКА СБОРКИ:"
                echo ""
                
                echo "📱 ВЕРСИИ ANDROID SDK:"
                grep -n "compileSdk" app/build.gradle | head -1 || echo "❌ compileSdk не найден"
                grep -n "targetSdk" app/build.gradle | head -1 || echo "❌ targetSdk не найден" 
                grep -n "minSdk" app/build.gradle | head -1 || echo "❌ minSdk не найден"
                echo ""
                
                echo "🔧 ВЕРСИИ GRADLE И TOOLS:"
                echo "Gradle Wrapper: $(grep distributionUrl gradle/wrapper/gradle-wrapper.properties | cut -d'/' -f6 | cut -d'-' -f2,3)"
                echo "Android Gradle Plugin: $(grep 'com.android.tools.build:gradle:' build.gradle | cut -d':' -f3 | tr -d "'")"
                echo "Java Version: $JAVA_VERSION"
                echo ""
                
                echo "📦 ВЕРСИИ CAPACITOR И ONESIGNAL:"
                echo "Capacitor: $(grep '@capacitor/android' ../package.json | cut -d':' -f2 | tr -d ' "^,')"
                echo "OneSignal: $(grep 'onesignal-cordova-plugin' ../package.json | cut -d':' -f2 | tr -d ' "^,')"
                echo ""
                
                echo "🔐 РАЗРЕШЕНИЯ В МАНИФЕСТЕ:"
                REPORT_MANIFEST="capacitor-app/android/app/src/main/AndroidManifest.xml"
                if [ -f "$REPORT_MANIFEST" ]; then
                  echo "  Всего разрешений: $(grep -c "uses-permission" "$REPORT_MANIFEST")"
                  echo "  POST_NOTIFICATIONS: $(grep -q "POST_NOTIFICATIONS" "$REPORT_MANIFEST" && echo "✓" || echo "✗")"
                else
                  echo "  ❌ Манифест недоступен"
                fi
                echo ""
                
                echo "📋 НАСТРОЙКИ ПРИЛОЖЕНИЯ:"
                if [ -f "$REPORT_MANIFEST" ]; then
                  echo "Package ID: $(grep 'package=' "$REPORT_MANIFEST" | cut -d'"' -f2)"
                  echo "App Name: $(grep 'android:label=' "$REPORT_MANIFEST" | head -1 | cut -d'"' -f2)"
                else
                  echo "❌ Не удается получить настройки приложения"
                fi
                echo ""
                
                echo "📱 ИНФОРМАЦИЯ О СОЗДАННОМ APK:"
                if [ -f "app/build/outputs/apk/release/app-release.apk" ]; then
                  echo "✅ APK успешно создан"
                  echo "📁 Размер: $(du -h app/build/outputs/apk/release/app-release.apk | cut -f1)"
                  echo "🔑 Подпись: $(jarsigner -verify -verbose app/build/outputs/apk/release/app-release.apk | grep "SHA1 digest algorithm" | wc -l > 0 && echo "Подписан" || echo "Не подписан")"
                  
                  echo ""
                  echo "🔍 ПРОВЕРКА РАЗРЕШЕНИЙ В ФИНАЛЬНОМ APK:"
                  if command -v aapt &> /dev/null; then
                    echo "Все разрешения в APK:"
                    aapt dump permissions app/build/outputs/apk/release/app-release.apk | grep "uses-permission" | while read line; do
                      echo "  ✓ $line"
                    done
                    
                    echo ""
                    echo "🎯 Проверка POST_NOTIFICATIONS:"
                    if aapt dump permissions app/build/outputs/apk/release/app-release.apk | grep -q "POST_NOTIFICATIONS"; then
                      echo "  ✅ android.permission.POST_NOTIFICATIONS НАЙДЕНО в APK"
                    else
                      echo "  ❌ android.permission.POST_NOTIFICATIONS НЕ НАЙДЕНО в APK"
                    fi
                  else
                    echo "  ⚠️ aapt недоступен для проверки разрешений"
                  fi
                else
                  echo "❌ APK не создан!"
                fi
                echo ""
                
                echo "🚀 ИТОГОВАЯ ГОТОВНОСТЬ К ТЕСТИРОВАНИЮ PUSH-УВЕДОМЛЕНИЙ:"
                echo "  ✓ Android SDK 34 (поддержка Android 15)"
                echo "  ✓ POST_NOTIFICATIONS разрешение объявлено в AndroidManifest.xml"
                echo "  ✓ Программный запрос разрешений в MainActivity.java"
                echo "  ✓ OneSignal SDK v5.2.13 с современным API"
                echo "  ✓ Двойной механизм запроса разрешений (native + OneSignal)"
                echo "  ✓ Разрешения проверены в финальном APK"
                echo ""
                echo "📋 ИНСТРУКЦИИ ДЛЯ ТЕСТИРОВАНИЯ:"
                echo "  1. Установите APK на устройство Android 13+ (API 33+)"
                echo "  2. При первом запуске появится системный диалог 'Разрешить уведомления?'"
                echo "  3. Нажмите 'Разрешить' для активации push-уведомлений"
                echo "  4. Проверьте работу через админ-панель приложения"
                echo ""
                echo "🔍 ДИАГНОСТИКА (если не работает):"
                echo "  adb logcat | grep -i 'POST_NOTIFICATIONS'"
                echo "  adb logcat | grep -i 'OneSignal'"
                echo "  adb logcat | grep -i 'Permission'"
                echo ""
                echo "🎯 РЕЗУЛЬТАТ: APK готов для полноценного тестирования push-уведомлений"
                echo "==============================================="

            - name: Find and rename APK
              run: |
                cd capacitor-app/android
                APK=$(find app/build/outputs/apk/release -name "*.apk" | grep -v unsigned | head -n 1)
                if [ -z "$APK" ]; then
                  echo "❌ APK не найден"
                  exit 1
                fi
                cp "$APK" "../../Loans-${{ github.ref_name }}.apk"
                ls -la "../../Loans-${{ github.ref_name }}.apk"

            - name: Display certificate fingerprint
              run: |
                echo "=== Проверяем подпись APK ==="
                APK_FILE=$(find capacitor-app/android/app/build/outputs/apk/release -name "*.apk" | grep -v unsigned | head -n 1)
                if [ -n "$APK_FILE" ]; then
                  echo "Найден APK: $APK_FILE"
                  # Проверяем подпись APK
                  jarsigner -verify -verbose -certs "$APK_FILE" || echo "APK не подписан!"
                  # Показываем сертификат keystore
                  echo "=== Сертификат keystore ==="
                  keytool -list -v -keystore capacitor-app/android/app/android-keystore.keystore -alias "${{ secrets.KEY_ALIAS }}" -storepass "${{ secrets.KEYSTORE_PASSWORD }}" | grep SHA1
                else
                  echo "APK не найден!"
                fi

            - name: Verify APK file
              run: |
                if [ ! -f "Loans-${{ github.ref_name }}.apk" ]; then
                  echo "❌ APK не найден!"
                  exit 1
                fi

            - uses: actions/upload-artifact@v4
              with:
                name: apk
                path: Loans-*.apk

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v1
              if: startsWith(github.ref, 'refs/tags/')
              with:
                files: Loans-*.apk
                draft: false
                prerelease: false
                name: "Займы онлайн ${{ github.ref_name }}"
                body: |
                  ### Займы онлайн (версия ${{ github.ref_name }})
                  - Браузер займов с фильтрами
                  - Админ-панель
                  - Push через OneSignal
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
